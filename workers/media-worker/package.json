{
  "name": "media-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Admin media CRUD Worker for R2",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260801.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.118.0"
  }
}
